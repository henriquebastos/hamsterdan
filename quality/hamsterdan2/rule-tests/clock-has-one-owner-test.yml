# Copyright (c) 2026 Henrique Bastos

id: clock-has-one-owner
valid:
  - "def renew(token, now=utc_now): return token.expires_at < now()"
  - "utc_now = partial(datetime.now, UTC)"
invalid:
  - "stamp = datetime.now(UTC)"
  - "today = date.today()"
  - "time.sleep(3)"
  - "await asyncio.sleep(1)"
