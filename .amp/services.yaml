services:
  hamsterdan-host:
    command: set -a; . .amp/runtime/hamsterdan.env; set +a; uv run --frozen python -m hamsterdan.host serve --host 0.0.0.0 --port "$PORT"
    port: 8000
    health: /healthz
