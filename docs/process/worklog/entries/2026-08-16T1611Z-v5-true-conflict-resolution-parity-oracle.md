# V5 true-conflict resolution parity oracle established

CV17.DS4.9 reconstructs historical live PR 31's user-visible conflict journey
in independent production and V5 worlds. A real local Git graph changes the
same fixture line differently on feature and base and requires `git merge-tree`
to prove conflict markers. A separate signed PR delivery enters durable pending
custody before either topology shows the unchanged feature head as stale,
unmergeable/conflicted, and not ready. No coding or conversation agent runs
merely because the conflict exists.

One explicit authorized owner comment selects `resolve_conflict` under the
current immediate-execution rule. Each topology sends one credential-free
coding operation the exact old head, current base, natural request, and
`merge_base=True`. The host applies the explicit line resolution, creates one
blob, tree, and ordered two-parent commit, and advances the feature ref through
one exact GraphQL compare-and-swap under stable lookup-first operation and
payload identity.

Before the resulting-head webhook, the dashboard remains on the stale conflict,
only old-head readiness exists, and all review work remains on the old head. A
fourth signed PR webhook is pending before admitting the new head. Both
topologies then observe successful run 102, clear new-head review, current and
mergeable dashboard, and new-head readiness. All four deliveries become
terminal and both worlds quiesce without findings, reruns, native review
comments, REST merge, duplicate Git objects, or a second ref update.

The complete topology-parity file passes with 20 tests. `scripts/check full`
passes with 1,093 Python tests, nine Bun relay tests, formatting, Ruff, typing,
and source/wheel builds. Deterministic parity remains intermediate; fresh
explicitly selected-V5 GitHub demonstrations are mandatory for acceptance.
