# Lifecycle and recovery portfolio qualified locally

CV3.DS3's bounded implementation is qualified locally without changing the
readiness Net, Petrus, Motus, AgentRunner, execution environments, provider
authority, package boundaries, credential custody, or the exact Petrus pin.

Persisted terminal Activity recovery now requires an exact unchanged canonical
History prefix plus the new terminal failure pair for a previously unresolved
occurrence before atomically replacing both Engine custodians and continuing
sibling work. A real persisted-History integration regression proves one
dashboard effect, explicit terminal outcomes, and no later append. Divergent
History and unproven runtime failures remain closed and propagate.

Immutable comments use exact marker-and-body recovery after uncertain calls,
retain a fresh authority fence before every possible mutation, retry only an
ambiguous transport outcome, and reject definitive provider responses and
marker collisions. Agent startup failure is sanitized. A disabled-by-default,
one-shot operation-scoped qualification seam exercises agent timeout/malformed
and comment pre-call/post-call failures, while a stable bounded
`inspect-instance` command inventories History without loading or replaying it.

Review found and corrected definitive-response retry, initial marker collision,
History-prefix divergence, and state-inspection path/identifier weaknesses.
Final full verification passed 237 Python tests with one opt-in provider test
skipped, all nine relay tests, lint, formatting, typing, sdist, and wheel builds.
No provider mutation, deployment, runtime restart, History edit, credential
movement, or accepted demo-PR mutation occurred. Fresh live qualification and
the bounded PR20 custody observation remain before CV3.DS3 acceptance.
