# V5 live campaign bootstrap qualified locally

The CV17 live campaign can now start from a fresh Hamsterdan Amp project orb
without copying person-named runtime files. Project role secrets provision and
verify distinct author and reviewer `gh` sessions, while public project
variables identify the exact App, HBNetwork installation, admitted repository,
workflow, and reminder policy. Setup captures and removes authority before any
installer, atomically creates only owned private files, installs the exact
qualified Pi and Node versions, and publishes a non-secret launch environment
only when the complete contract is present.

The supervised host now enters through a checked launcher rather than sourcing
shell text. It holds verified non-symlink descriptors for `.amp` and its runtime
directory while reading the exact environment and mutating the private topology
selector, strips ambient Amp, GitHub, and provider authority, preserves only the
explicit qualification-fault seam, and defaults to production. Setup verifies
each role through the fixed system GitHub CLI in an empty environment. The host
Git publisher now runs Git in a closed environment that cannot consult ambient
human credential helpers. A private local selector admits only V5 or restores
production; unknown values fail closed. Focused tests cover disclosure,
authority retirement, installer failure, final-file and parent-directory
symlink custody, PATH-precedence wrapper refusal, ambient-secret and Git-helper
isolation, launcher modes, and topology selection. `scripts/check full` passes
with 1,107 Python tests, nine Bun relay tests, formatting, Ruff, typing, and
source/wheel builds.

Real read-only preflight verified project role identities `henriquebastos` and
`crisbastos`, App `hamster-dan` ID `4452953`, HBNetwork ID `108842540`, and
`HBNetwork/demo-pr-readiness` ID `1316665126`. Public App inventory still lacks
the required review-thread event. The archived historical runtime retains the
accepted App key and webhook secret but is unavailable for file transfer while
archived, and no project Anthropic key exists. Setup therefore produced no
launch environment and no host or GitHub mutation occurred. Fresh selected-V5
acceptance remains blocked on projectizing those three authorities and updating
the App event subscription and relay URL.
