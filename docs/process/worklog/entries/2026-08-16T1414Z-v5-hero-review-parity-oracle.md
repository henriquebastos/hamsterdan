# V5 hero-review parity oracle established

CV17.DS4.6 completes the deterministic content portfolio with the review-only
moment recovered from the historical three-actor hero. Production and V5 each
receive green exact-head CI and one credential-free coordinating review with
the same three blocking defects: a seconds/minutes lease bug with a replacement
suggestion, a conceptual approval-policy bug, and a cache-key capitalization
bug with one related location. The complete validated findings and new lineage
remain correlated to generation 1 and its admitted head. The fake agent emits a
JSON-shaped result through the real result validator rather than directly
constructing an already-admitted domain result.

Production proves the three native GitHub review shapes at the exact commit:
one suggestion, one conceptual inline comment, and one multi-location inline
comment. V5 proves the same actionable identities, titles, explanations,
severity, locations, evidence, replacement, and relationship in one App-owned
batch comment. The oracle accepts this presentation difference rather than
comparing traces or comment bytes, while requiring each defect to be visible
exactly once under a stable head-bound effect.

A second independent fresh same-head provider and host world produces the same
agent and finding-publication operation identities. The replay shares no
durable state with the first world, so the oracle demonstrates globally stable,
settleable work identities rather than one-process coincidence.

Both topologies retain one blocked current dashboard, publish no readiness, and
quiesce without coding, conversation, rerun, Git/ref mutation, or merge work.
Repair, base update, thread resolution, and human approval remain assigned to
the lifecycle and collaboration portfolio rather than hidden inside this
content slice.

The focused parity, production finding, and V5 gate checks passed with 64 tests.
`scripts/check full` passed with 1,079 Python tests, nine Bun relay tests,
formatting, Ruff, typing, and source/wheel builds. Fresh selected-V5 GitHub
demonstrations remain mandatory for CV17 acceptance.
