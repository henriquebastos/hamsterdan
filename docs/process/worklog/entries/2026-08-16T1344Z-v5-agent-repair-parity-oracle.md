# V5 agent-repair parity oracle established

CV17.DS4.5 extends the real-host semantic harness with the repair journey
recovered from historical PR 28. Production and V5 each observe failed exact-
head CI attempt 1, publish one brokered rerun, admit failed attempt 2 through
signed durable webhook custody, and only then invoke one credential-free coding
repair with exact failure evidence. The controlled repair produces a real patch
restoring the missing mergeability guard identified by the seeded finding.

The journey replaces the earlier publication seam with the actual
`HostGitPublisher` against a real local repository while retaining the normal
public clone URL at composition and agent boundaries. It proves lookup-first
reconciliation, one host-created blob/tree/commit set, one exact old-head ref
CAS, the expected parent and commit trailers, changed repository content, and
provider/ref agreement. A distinct successful run 102 belongs to the repaired
head. That head remains unannounced until a third signed delivery is durably
pending and admits generation 2.

Both topologies then perform one repaired-head review, resolve the seeded
finding through lineage, update the current dashboard, publish one terminal
readiness advisory, and quiesce without duplicate Git publication or merge.
The oracle preserves each topology's stable global operation identities and
the user-visible outcome without requiring internal trace or rendering
identity.

The focused parity, Git publication, and V5 mutation checks passed with 81
tests. `scripts/check full` passed with 1,077 Python tests, nine Bun relay tests,
formatting, Ruff, typing, and source/wheel builds. Deterministic evidence remains
intermediate; fresh selected-V5 GitHub demonstrations are still mandatory for
CV17 acceptance.
