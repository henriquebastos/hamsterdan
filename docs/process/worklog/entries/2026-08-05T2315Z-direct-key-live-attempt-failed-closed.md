# Direct-key live attempt failed closed at Git publication

The separately authorized CV16.DS11 live gate ran once through production
`HostService`, Application, Activity, unchanged `AgentRunner`, Petrus
`PiA2RuntimeHost`, private workspace receiver, and real `HostGitPublisher`.
The selected route was exactly Pi native A2 Local with direct Anthropic API-key
authority and `claude-sonnet-4-5`; no Amp, A4/A5, subscription, extra-usage, or
ambient-provider fallback was used.

The direct key arrived through a fresh RSA-OAEP-SHA256 encrypted handoff. Canary
and authority admission passed without rendering or hashing plaintext. The
production one-shot supplier materialized authority once, after which the
qualification key was removed. Sanitized attempt ledger: one attempt, zero
retries, one authority materialization, provider call begun, zero recorded
operations, zero recorded Hands calls, one returned mutation candidate, and
zero publications.

The candidate reached the host publication path, where `HostGitPublisher`
failed closed with `GitPublishError`. Route and correlation assertions passed;
the coding request preceded terminal canonical History. Schema, exact result
tree, replay/no-republish, and aggregate cleanup acceptance did not pass because
the route stopped at publication. No ref advanced and the deterministic-stop
rule prohibited retry.

Independent terminal inspection found no qualification key, Petrus custody key,
operation workspace, Hands stage, or Pi process. Hamsterdan local, tracking, and
remote `main` all remained at `05df1d2f762a0e6b15ca311584c2088c1883347f`
before this evidence update, with a clean worktree. All local qualification,
harness, identity, and handoff roots were erased. The qualification identity
could not delete, privatize, or archive the disposable remote repository, so
that remote cleanup and the exact credential-free publication diagnosis are
recorded as blocking debt.

The previously completed deterministic evidence remains 101 focused Python
tests and the full gate: 332 Python tests with one explicit provider test
skipped, nine Bun tests, formatting, Ruff, ty, and source/wheel builds. This
attempt does not establish exactly-once provider execution, forensic erasure,
complete migration, or live support. CV16.DS11 remains Qualified Locally and is
not Done.
