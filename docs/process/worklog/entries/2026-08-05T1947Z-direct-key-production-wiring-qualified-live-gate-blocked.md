# Direct-key production wiring qualified; live gate blocked before attempt

Hamsterdan production now composes the exact Pi native A2 Local route from
absolute installation-owned Pi CLI, Node, and package paths. Its direct
Anthropic key is accepted only from an explicit owned, bounded `0600` regular
file and is read lazily into the erasable one-shot supplier. Startup and API
lifespan failure paths close already-owned runtime custody. No provider or
client-factory conformance seam enters production.

Focused direct-key, production wiring, workspace, adapter, architecture, and
service tests passed 101 tests. `scripts/check full` passed formatting, Ruff,
ty, nine Bun relay tests, source and wheel builds, and 332 Python tests with the
one explicit provider test skipped. An exact installed Pi 0.83.0 and Node 22.19
probe selected `pi.native.a2.local` and `PiNativeRunner` without requesting
authority.

Commit `c368fefab686840c557150833c77ce0acf1c7382` was pushed before the live
gate. A fresh isolated clone matched both local and remote `main`, installed
from the frozen lock, remained clean, and repeated the authority-free READY
production probe. The first authorized authority check then found no configured
direct Anthropic key. The gate stopped before operation or provider work rather
than substitute another provider, Amp, native subscription, or extra usage.

Attempt ledger: zero attempts, zero retries, zero provider calls begun, zero
operations, zero Hands calls, zero mutations, and zero publications. Runtime
probe cleanup was verified and all qualification roots were removed. No
credential, prompt, transcript, model output, provider diagnostics, or private
identity was retained. DS11 remains Qualified Locally and is not Done.
