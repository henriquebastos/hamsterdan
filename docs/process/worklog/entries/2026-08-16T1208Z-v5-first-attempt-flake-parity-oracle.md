# V5 first-attempt-flake parity oracle established

CV17.DS4.1 extends the real-host semantic harness with the historical PR 24
meaning: required CI fails on attempt 1, Hamsterdan publishes one exact
operation-bound rerun marker, the same run succeeds on attempt 2, and no repair
runs. Production and V5 execute in independent strict fake GitHub worlds; the
test compares neither their internal states nor traces.

Attempt 2 enters through its own signed `workflow_run` webhook and durable
custody. Both topologies read required-job evidence for both attempts, retain
one dashboard, publish one readiness advisory at the admitted head, perform
one credential-free clear review, and quiesce with two terminal deliveries.
Neither invokes coding or conversation agents, publishes findings, repeats the
rerun, mutates Git refs, or merges.

The focused rerun and parity portfolio passed with 34 selected tests.
`scripts/check full` passed with 1,067 Python tests, nine Bun relay tests,
formatting, Ruff, typing, and source/wheel builds. This deterministic oracle is
intermediate evidence; the fresh selected-V5 live GitHub campaign remains
mandatory for CV17 acceptance.
