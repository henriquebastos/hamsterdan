# Native review hero qualified locally

CV4.DS1 is qualified locally. The current reviewer OAuth account was verified
as `crisbastos`, its Amp project secret and ignored runtime custody replaced the
former `hsbastos` names, and the active setup/operator/test paths now use the new
login. Accepted historical records retain the login GitHub reported when those
events occurred.

Review and conversation agents now receive the Navigator-supplied Dan voice in
full. Deterministic dashboard, status, reminder, readiness, malformed-request,
and guarded-mutation messages use the same register while preserving stable
markers, operation IDs, and exact digest confirmation.

Findings publish as lookup-first native PR review comments at the exact current
head. One-line replacements render as GitHub suggestions; conceptual findings
remain ordinary inline comments; one conceptual defect may carry bounded,
clickable exact-head related locations without becoming duplicate operations.
Only an exact changed-line rejection permits issue-comment fallback. Provider
authorization, missing-resource, and generic validation responses fail closed.
Legacy issue findings, reminders, and readiness advisories remain exactly
recoverable across deployment replay, and marker-safe identities plus final-line
matching prevent agent prose from impersonating trusted effects.

The separate demo repository merged its closed multi-file `hero-review` fixture
through PR45 at merge SHA `51a265b0948c2b797f69f3c5f54f8d6b557f2592`.
All six provider checks passed. The fixture seeds one seconds/minutes replacement,
one approval-policy defect, and one two-location cache-key invariant while its
normal executable contract remains green.

Hamsterdan verification completed with 258 tests passed and one opt-in provider
test skipped; all nine relay tests passed. The demo fixture completed 61 tests
with two prepared-state skips, plus Ruff and ty. Deployment and one fresh live
three-actor rehearsal remain before acceptance.
