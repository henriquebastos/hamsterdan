# CV21 first bridged lifecycle completed locally

CV21.DS1 now has a non-selectable first vertical pulse under
`src/hamsterdan2`: strict host command and durable subject/root custody, one-PR
readiness runtime, the sole bridge-mounted retained V5 Net and seed, and a
detached `awaiting_observation` host record at the `host_recorded` cut. It uses
fresh disposable state and adds no provider observation, Worker, concrete
Activity execution, current-state reader, runtime selector, or deployment.

Owner-local and root Petrus version-4 Worlds execute the real Engine, History,
Dispatch, and bridge, report finite resources, preserve action and checker
identity, and exactly replay from fresh roots. Independent checker mutations of
subject, action, and bridge identity fail. Encoded artifacts contain no current
module or V5 contract names.

Public Petrus process sessions prove actual `SIGKILL` reconstruction on both
sides of `host_recorded`. Before the cut, readiness History survives while the
host transaction rolls back; after the cut, reconstruction is idempotent.
Readiness uses Petrus's fenced SQLite Engine constructors, whose
database-and-Instance writer fence refuses a concurrent Engine before History
load. The catalog transaction additionally serializes the normal composed host
path across readiness. Killing the holder releases host custody and a fresh
process reconstructs from the same 21-record canonical History.

The isolated `scripts/check hamsterdan2` profile applies strict Ruff and
formatting, ty, seven verified ast-grep rules, architecture and feedback
contracts, and replacement tests only to `src/hamsterdan2` and `tests2`. The
architecture contract pins the sole legacy bridge allowlist, acyclic imports,
empty package facades, readiness runtime custody of Engine/History/Dispatch,
simulation isolation, sole concrete host construction, and absence of Worker
construction. The replacement gate completed with 30 tests passing.
