# V5 collaboration approval parity oracle established

CV17.DS4.10 reconstructs historical live PR 33's unchanged-head collaboration
journey in independent production and V5 worlds. Green exact-head CI and a
clear coordinating review remain blocked while four real-shaped signed events
enter durable custody in order: reviewer requested, changes requested with one
unresolved inline thread, distinct non-author approval while the thread remains
open, and native review-thread resolution. The dashboard shows each human gate
transition and no readiness exists until the final event. All five deliveries
become terminal and both worlds quiesce with one ready advisory.

The scenario exposed an invalid review-comment wake-up assumption. GitHub sends
`pull_request_review_thread` with `resolved` or `unresolved` when thread state
changes; it does not edit the review comment. Hamsterdan now includes that event
in durable webhook custody and the required GitHub App registration contract,
and extracts its PR number through the existing neutral observation boundary.
Production and V5 topology code remain unchanged. Registration inventory fails
closed until a live App subscription includes the required event.

Both topologies run one clear review and no coding, conversation, rerun,
finding, Git/ref, or merge effect. The complete topology-parity file passes
with 22 tests. `scripts/check full` passes with 1,096 Python tests, nine Bun
relay tests, formatting, Ruff, typing, and source/wheel builds. The selected
deterministic portfolio is complete; fresh explicitly selected-V5 GitHub
demonstrations remain mandatory for CV17 acceptance.
