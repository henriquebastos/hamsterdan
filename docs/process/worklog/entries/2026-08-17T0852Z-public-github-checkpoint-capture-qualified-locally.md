# Public GitHub checkpoint capture qualified locally

CV17.DS4.25 adds a credential-free evidence lane distinct from the synthetic
Remotion studio. Its closed-schema PR 61 manifest identifies fourteen surviving
GitHub checkpoints across the human reviews and requests, App findings, repair
commit, repaired-head checks, recovery, conversation, and readiness. A fresh
browser validates exact rendered provider identity and bounded text before
atomically publishing screenshots and a provenance report.

The lane blocks mutation-capable requests and closes the browser before final
classification. Fixture capture and rendering retain an unmistakable fixture
kind that the live renderer rejects. Both stages bind the exact manifest bytes;
rendering rejects unsafe filesystem inputs and gives `ffmpeg` only private
copies of already hash-verified screenshots. The resulting montage is a silent
direct cut of real GitHub UI without reconstruction, overlays, or transitions.

Thirty-eight focused TypeScript tests pass, including late mutation, fixture
provenance, mutable manifest, path escape, symlink, and direct-child boundary
regressions. Quick and full checks pass with 1,183 Python tests and nine Bun
relay tests. A fresh anonymous development probe validated all fourteen PR 61
checkpoints and rendered a 76-second 1440×900 silent H.264 montage; complete
media inspection found no blank, challenge, error, or synthetic frame. Oracle
returned `clear to commit` after its findings were applied.

The development probe is not canonical acceptance evidence: the production
command correctly refuses a dirty worktree. Clean committed-source capture,
final inspection, and durable publication follow after this source slice.
