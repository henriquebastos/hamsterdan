# DS11 agent-result admission observation qualified locally

Starting from exact Hamsterdan `01004c3285f7a81de0b878fab918ef8e2f8730a3`,
this credential-free slice closed the retained evidence gap exposed by the
second blocked live attempt. It used no key, subscription, provider operation,
repository mutation, fresh attempt, or DS12 work.

The unchanged public `AgentRunner` request/result protocol now crosses a narrow
typed observation boundary. Pi coding retains exactly one closed result outcome:
`runtime_lifecycle`, `output_schema`, `correlation`, `unchanged`, `unable`, or
`workspace_reconciliation`. Missing or wrongly typed identity is schema failure;
well-typed differing identity is correlation failure. Runtime-operation and
workspace cleanup uncertainty is retained separately and cannot overwrite the
first cause. Canonical Activity History contains only enum values, never model
output, provider diagnostics, exception prose, coordinates, refs, URLs, patch
text, or native identity.

Every categorized failure and valid non-mutation stops after one dispatch with
zero `HostGitPublisher` call, publication, or replay. Generic unclassified
transport failure preserves the existing bounded same-route retry fence. An
accepted changed result still uses the host-reconciled canonical patch and the
unchanged current-authority, object-identity, exact-CAS, and idempotent
publication path. Qualification rejects malformed or arbitrary category values,
suppresses publication assertions after zero publication, and keeps original,
replay, and cleanup evidence independent.

Focused adapter, Activity, qualification, and Application tests pass 91 tests.
The full project gate passes formatting, Ruff, ty, nine Bun tests, source and
wheel builds, and 369 Python tests with one explicit provider test skipped.
Review found and closed combined preparation/cleanup masking, throwing runtime
identity/cancel collaborator, and falsy-category sanitation edge cases. No new
durable debt was accepted, and resolved orphan cleanup remains resolved.

This result does not reclassify either historical blocked attempt: the second
attempt remains only `agent_result_unaccepted`. DS11 remains Qualified Locally /
BLOCKED. The deterministic code is eligible to request a fresh separately
bounded provider attempt, but this slice neither requests nor authorizes it and
does not begin DS12.
