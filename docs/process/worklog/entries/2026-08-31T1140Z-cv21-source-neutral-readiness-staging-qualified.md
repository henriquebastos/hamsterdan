# CV21 source-neutral readiness staging qualified locally

CV21.DS2 task 3 now runs one authority-side staging turn outside the signed
webhook request. The sole host composition root reconstructs task-2 delivery
custody and asks readiness to project one workflow-owned Head observation,
canonicalize its complete source-neutral semantics, derive `ObservationKey` v1,
and atomically retain one immutable manifest, manifest-scoped grant, ordered
entry, and closed classification. Detached fresh-process reconstruction checks
the canonical acquisition, key/bytes relation, entry order, manifest, grant,
and decision without admitting anything to Petrus History. An immutable
transaction sequence replays preceding staging evidence and exactly recomputes
each decision without treating acquisition order as Head semantic order.

Behavioral evidence covers novel staging, exact reoffer, distinct-acquisition
corroboration, quarantined empty acquisition collision, semantic collision,
same-tip conflict, incomparable changed Head, diagnostic-field exclusion,
changed acquisition facts, strict mutation rejection, finite capacity and
resource reporting, and real SQLite concurrency. `stale` remains in the closed
family for later orderable observations without timestamp-ordering Head
evidence.

Readiness-owner and cumulative Petrus Worlds replay deterministically from
fresh roots under explicit budgets. The root first proves HTTP custody alone
leaves ingress and History empty, then stages exactly once through a separate
command while History, retained fold, and Dispatch remain untouched.
Independent mutations of acquisition, manifest, entry order, key, canonical
bytes, grant, sequence, and decision fail the checker. Owner/root evidence also
pins the complete detached acquisition, while exact and changed HTTP
redeliveries fail closed on corrupted host custody without mutation. Actual
`SIGKILL` after the staging transaction but before caller success leaves one
reconstructible authority; a fresh composition reoffers it as
`exact_duplicate` without another manifest, grant, entry, or decision.

Real SQLite overlap evidence proves HTTP custody completes while one staging
turn holds the readiness transaction and another waits. Staging linearizes at
a final bounded host custody read following strict reconstruction: a collision
during reconstruction is observed as empty fatal staging, while a collision
after that read cannot rewrite the staged acquisition. Readiness locks staging
turns before host selection, preventing a later quarantine selector from
overtaking an earlier eligible selector while leaving HTTP independent of that
lock. Reconstruction caps each rowset before decoding and rejects custody
above its configured manifest or effective SQLite page ceiling. Eligible and
quarantined first-acquisition paths both reject corrupt prior or orphan child
authority before mutation; host delivery custody above its configured row or
effective page ceiling is rejected before it can feed readiness. Composition
forwards the host row ceiling, and malformed-schema evidence proves duplicate
host acquisition, readiness acquisition, or grant rows fail closed.

The 135 task-focused tests passed. The isolated replacement gate passed with 211
tests after strict Ruff, formatting, ty, seven ast-grep rule fixtures and scans,
architecture checks, owner/root replay, process recovery, concurrency, and
resource evidence. The full profile passed every preceding gate, relay,
TypeScript, live-capture, build, and task-related test; its cumulative Python
run reproduced only the inherited 14 `tests/unit/test_orb_setup.py` failures at
`.agents/setup:229` from unset `$USER`, alongside 1,151 passes. This task does
not alter that unrelated baseline.

The task leaves the Hamsterdan Petrus pin unchanged and stops before bridge
conversion, History acceptance, retained-Net fold, host completion, Dispatch,
Worker, provider effect/read, agent work, or workflow advancement. CV21.DS2
remains Active; task 4 is next.
