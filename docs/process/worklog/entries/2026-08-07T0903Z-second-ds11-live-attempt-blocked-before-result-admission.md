# Second DS11 live attempt blocked before changed-result admission

The Navigator separately authorized one final bounded direct-key qualification
from exact Hamsterdan `965a5be45c8a8bb232ea7737bc626789718a355f`
and Petrus `750c4321bb51563666ce43e88f753ebe1f068520`. Credential-free
preflight created one exact disposable target, proved production-compatible
object/ref identity and current/stale exact CAS behavior, and left the direct key
unopened. A fresh isolated installed-package probe selected only Pi native A2
Local, direct Anthropic API-key authority, and `claude-sonnet-4-5` through
explicit absolute Pi and Node paths.

The production `HostService` → Application → Activity → unchanged AgentRunner
adapter → `PiNativeRunner` → `PiA2RuntimeHost` → private receiver route completed
one operation. Sanitized ledger: fresh attempt one, cumulative DS11 authority
attempt two, zero retries, one authority materialization, one provider
operation, six Hands calls, one accepted output append, zero changed mutation
candidates, zero publications, and zero replays. No changed `CodingResult`
crossed admission, so `HostGitPublisher` was never called. The first safe
category is `agent_result_unaccepted`; erased raw output and provider diagnostics
do not support a narrower inference. Publication-dependent schema, tree, and
replay assertions correctly remained suppressed.

Operation cleanup preceded terminal History, and terminal History preceded
route settlement. The direct key was removed after one-shot DS2 consumption.
Runtime close left reauthorization required, zero live leases, no persistent
custody key, materialization, process, Attachment, workspace, Hands stage,
handoff, or qualification root. The bounded identity lacked repository-delete
authority, so the Navigator deleted only the exact target; qualification
verified not-found and erased the final mode-`0600` coordinate custody. No
repository enumeration or unrelated mutation occurred.

DS11 remains Qualified Locally rather than Done. The next deterministic work is
a closed, provider-neutral agent-result admission category that separates
lifecycle, parse/schema/correlation, unchanged-result, and workspace-proof
failure without retaining model/provider prose. No additional provider attempt
or DS12 work is authorized by this result.

The final unchanged-code gate passes formatting, Ruff, ty, nine Bun tests,
source and wheel builds, and 343 Python tests with one explicit provider test
skipped. Roadmap, decision consequences, briefing, debt, and worklog agree on
the blocked status and resolved remote cleanup.
