# V5 clean-green parity oracle established

CV17.DS4 began with one user-facing semantic scenario rather than an internal
trace comparator. The same strict clean-green fake GitHub world now enters
durable `HostService` webhook custody independently for production and V5.
Each topology reaches one App dashboard and one ready advisory after one
credential-free clear review, then quiesces with terminal custody and no ref,
merge, rerun, finding, coding, or conversation mutation.

The strict provider fixture exposed a V5 parity defect before live
qualification: strict-base evidence was never obtained, so V5 could call a
stale PR ready. V5 now freezes the provider compare result with the policy,
mails strict/current evidence through lifecycle, and fail-closes readiness
until a strict base is current. A same-head refresh clears the gate without a
second agent review; non-strict policy remains unaffected. Older retained V5
tokens and manifests lacking the new evidence remain readable without changing
their persisted payload or digest, but fail closed until host reconciliation.
An unlanded pre-upgrade readiness request settles moved without posting; an
already-landed request still reconciles before the new fence.

The focused V5 and parity portfolio passed with 439 tests. `scripts/check full`
passed with 1,065 Python tests, nine Bun relay tests, formatting, Ruff, typing,
and source/wheel builds. Deterministic scenarios remain intermediate evidence:
CV17 stays active pending the broader semantic catalog and a fresh selected-V5
campaign on `HBNetwork/demo-pr-readiness` with recorded real PR, Actions, App,
review, and human-interaction evidence. Historical production PRs do not count
as fresh acceptance.
