# Authority and collaboration folding qualified locally

CV3.DS2's minimal GitHub-authority folding and provider-evidence route are
qualified locally without changing the readiness Net. Real requested-review,
changes-requested, approval, unresolved-thread, base, conflict, and lifecycle
facts continue to fold into the existing neutral Control fields and one
priority-ordered external wait.

Characterization found two boundary defects. Author approvals are now excluded
with GitHub's case-insensitive login semantics. Reversible observations such as
clean → conflict → clean now compare against current normalized Control and use
its durable revision to admit a legitimate restored state. Restart coverage
proves the restoration is accepted once and an identical later poll changes
neither History nor provider publications.

The new read-only `inspect-authority` command collects bounded, prose-free
provider facts and revalidates exact head/base authority after collection. It
uses a parity-tested copy of production's GitHub rules normalization without
crossing sibling-package boundaries, fails closed on potentially incomplete
collections and indeterminate mergeability, includes user and team review
requests, and uses GitHub GraphQL `reviewDecision` rather than local approval
counting as required-approval evidence.

Final full verification passed 218 Python tests with one opt-in provider test
skipped, all nine relay tests, lint, formatting, type checking, sdist, and wheel
builds. A read-only live inspection of accepted PR23 passed on one stable
current GitHub basis. No demo repository mutation, source commit, deployment,
credential movement, History edit, or change to accepted PR14/15 or PR23-28
occurred. Fresh isolated provider qualification remains before CV3.DS2
acceptance.
