# Deterministic readiness World accepted

The Navigator accepted CV18.DS2's debugger-like `ReadinessWorld` and
`ReadinessTimeline` around the real production-default `HostService`. The
strict vertical sets independent PR, CI, and review truth; emits and delivers a
signed webhook through real ASGI custody; runs until GitHub has accepted one
readiness effect whose response was lost; abruptly drops the runtime generation
before projection; reconstructs from durable custody, canonical Petrus History,
Motus Dispatch, and runnable state; and converges lookup-first without a second
provider acceptance.

Hand-authored Timeline stories and exact artifact replay use the same
`petrus.testing.dst/v1` command interpreter. Petrus owns deterministic logical
time, order, budgets, checker cadence, crash generations, fair phase, and the
`petrus-dst-world` v1 artifact. Hamsterdan owns strict domain commands, modeled
provider truth, boundary-faithful adapters, detached host observations, and the
independent expected-readiness checker. Unknown provider requests and unsupported
commands/faults fail closed; no Petrus private runtime object or credential
enters observations or artifacts.

Slice-boundary review found that the first bounded custody door reported only
that an attempt occurred. It now returns detached delivery/disposition data,
and the checker compares that result with the independently modeled action.
Follow-up review then found an unaddressed delivery could be mislabeled
`completed` after an application was loaded; host selection now reports it as
`disposed`, and host plus whole-World regressions prove neither retained nor
disposed custody can be normalized as successful semantic admission. Final
focused host/World verification passed 86 tests, and follow-up Oracle review
reported no remaining blocker for that finding.

`scripts/check full` passed 1,220 Python tests, 44 media tests, nine webhook
relay tests, static/type/architecture checks, and source/wheel builds. Production
remains the default; V5 is not selected; Petrus was not modified.

The accepted vertical is plumbing proof, not generated semantic coverage. DS3
owns remaining DS1 fault adapters beside the generated dimensions that exercise
them, independent checker expansion, stateful generation, and shrinking. The
current coherent vertical module must split provider adapters from
profile/checker/Timeline ownership before that expansion; the debt ledger owns
the trigger. Actual process-kill and real-provider correspondence remain DS4
evidence.
