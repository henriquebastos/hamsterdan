# V5 mutation-route restart corrected locally

The first selected-V5 host restart after the Pi coding-boundary correction
failed closed before serving traffic. Agent-route startup repair found PR 59's
retained terminal `mut.git_gate`, but rejected its valid topology-labeled V5
binding because the repair helper independently required the older three-field
production shape. No History record, provider operation, or Git state was
rewritten or replayed.

Mutation-route reconstruction now uses the canonical bounded instance-binding
reader. It accepts topology-labeled V5 and legacy production bindings while
retaining exact path-to-instance and pull-request agreement plus repository
validation before reconstructing the globally scoped operation identity. The
terminal-route test now proves the real V5 shape; the fault-route test continues
to prove the legacy shape.

Fifty focused agent, binding, startup, and restart tests passed, followed by
quick checks, 1,171 Python tests, and nine Bun relay tests. The Oracle commit
ruling and a restart against retained PR 59 state remain before this correction
is operationally qualified.
