# V5 persistent-CI parity oracle established

CV17.DS4.2 extends the real-host semantic harness with historical PR 25's
GitHub-visible meaning. Required CI fails on attempt 1, Hamsterdan publishes
one exact rerun marker, and a second signed `workflow_run` delivery exposes the
same run's failed attempt 2 only after durable pending custody.

Production and V5 each perform one credential-free repair investigation bound
to the admitted repository, PR, head, base, failed run, attempt, conclusion,
and fingerprint. The controlled coding result is `unchanged`: the single
dashboard updates, readiness stays absent, and no finding, second rerun, Git
publication, ref mutation, merge, or conversation occurs. V5's mandatory
pre-agent operation lookup uses a deterministic absent-operation seam and Git
publication fails the test if reached; the agent-repair scenario still owns
real Git object and exact-CAS evidence.

The oracle preserves the user's visible outcome rather than matching the prior
campaign's internal count of agent attempts. The focused escalation, mutation,
and parity portfolio passed with 118 selected tests. `scripts/check full`
passed with 1,069 Python tests, nine Bun relay tests, formatting, Ruff, typing,
and source/wheel builds. Fresh selected-V5 live GitHub demonstrations remain
mandatory for CV17 acceptance.
