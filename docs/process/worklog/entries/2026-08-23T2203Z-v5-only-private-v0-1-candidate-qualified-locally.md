# V5-only private v0.1 candidate qualified locally

Hamsterdan now has one runtime topology. `HostService` owns non-sharded V5
directly; the former production composition, descriptor, selector, source, and
parallel parity tests are removed. The runtime rejects every presence of the
retired selector and refuses former-production or unlabeled durable roots
without mutation. Explicit V5 roots and the bounded V5-internal input migration
lane remain supported. The eleven accepted semantic journeys now execute as the
sole-V5 scenario portfolio.

Durable reply, dashboard, and readiness publication names now have one allowlist
shared by gate binding, composite dispatch, unresolved-publication inspection,
Worker composition, and host recovery. The accepted CV18 semantic coverage
accounting is recovered and indexed. Broader CV18 campaigns and design
explorations are paused behind private production; CV16 closes through CV17
PR61's changed-result, App-authored Git, exact-CAS, lookup-first recovery, and
readiness evidence. CV19 is the one active production owner.

Orb setup installs the private pinned Petrus source through a dedicated
project-scoped `PETRUS_GITHUB_TOKEN`, temporary mode-restricted Git askpass and
configuration, and cleanup on every outcome. Optional demo-human identities no
longer gate a complete App/provider runtime. Two consecutive real setup runs
completed the private Petrus fetch, locked Bun 1.3.10 install, Playwright
Chromium setup, Pi Node 22.19.0/Pi 0.83.0 installation, and private runtime
materialization. The unreliable Remotion browser download is removed from
production setup without removing the secondary studio, locked Bun checks, or
Playwright.

Package and lock metadata now identify the private `0.1.0` candidate.
`uv sync --frozen` replaced the installed editable `0.0.0` package with
`0.1.0`. `scripts/check full` passed Ruff, formatting, ty, 9 webhook-relay
tests, 44 media tests, source/wheel builds named `hamsterdan-0.1.0`, and 1,106
Python tests in 239.77 seconds. Focused verification also passed 17 orb-setup
tests and 353 host tests; shell syntax, frozen setup, quick checks, and diff
integrity are green.

This is local candidate evidence, not deployment or release. The candidate host
has not been launched, the exact production repository is not selected, and the
existing private App can target only HBNetwork-owned repositories. GitHub-hosted
Actions remains blocked before job start by the account's payment/spending
state. No commit, push, tag, release, repository-visibility change, or production
effect was performed. A separate post-production debt item owns the detailed
open-source audit and the decision to retain or remove Remotion.
