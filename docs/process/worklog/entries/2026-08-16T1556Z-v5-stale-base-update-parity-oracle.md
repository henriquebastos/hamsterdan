# V5 stale-base update parity oracle established

CV17.DS4.8 reconstructs historical live PR 30's user-visible stale-base
journey in independent production and V5 worlds. After initial clean-green
readiness, the controlled provider advances the strict base branch without
moving the cleanly mergeable feature head. A separate signed PR webhook enters
durable pending custody before either topology observes the new base, updates
the singleton dashboard to not ready, and settles without coding or
conversation work.

One explicit authorized owner comment then selects `update_base`. Each
topology sends one credential-free coding operation the exact feature head,
current base, natural request, and `merge_base=True`. The host applies the
agent's patch against a real local Git remote, creates one blob, tree, and
ordered two-parent commit, and advances the feature ref through one exact
GraphQL compare-and-swap. Lookup-first reconciliation retains one globally
scoped operation and payload identity through the absent and landed states.

A fourth signed PR webhook is pending before admitting the resulting head.
Both topologies then observe successful run 102, clear exact-head review,
current-base dashboard, and new-head readiness. All four deliveries become
terminal and both worlds quiesce without findings, reruns, native review
comments, REST merge, duplicate Git objects, or a second ref update.

The focused topology-parity file passes with 18 tests. `scripts/check full`
passes with 1,091 Python tests, nine Bun relay tests, formatting, Ruff, typing,
and source/wheel builds. This deterministic proof remains intermediate; fresh
explicitly selected-V5 GitHub demonstrations are still mandatory for CV17
acceptance.
