# 1. PR83 full hero journey with initial summary first

[PR83](https://github.com/HBNetwork/demo-pr-readiness/pull/83) passed the full
three-actor journey on the deployed startup fix. It was closed unmerged after
capture. This adds a complete hero run to the narrower PR82 startup proof.

Production remained on revision `2f1b646906bcd8e3b13f6b969eed808f6d70e905`,
image `sha256:441d880dd0036df4860179e96228e5f38abe189a02bbda9343bbf5acf9bfc49e`,
Petrus `4e5c2500af4eb439e8e8f5ec108982c81bfc7427`, and OpenAI `gpt-5.6-sol`.
No runtime change or deployment was needed for this run. The deployed release
had already passed manual provider proofs and 1,267 parallel plus 1,267 serial
tests, with 18 declared platform deselections.

## 1a. Observed journey

1. The author opened head `d6e243889110b25298b7e96b4f684b9922097f1a` at
   23:44:38Z on 2026-09-04, with Cris requested as reviewer. The initial summary
   appeared at 23:44:52Z, fourteen seconds later. History records `DashLanded`
   at sequence 318, startup completion at 341, and the first normal Activity
   request at 345. The initial still shows "Dan's review: reviewing" before
   findings appeared.
2. All six opening CI jobs passed. Review `5118743948` contained exactly three
   App inline comments: seconds treated as minutes at `gate.py:10`, false
   approvals counted at `gate.py:16`, and inconsistent cache normalization at
   `cache.py:11`, related to `cache.py:6`. The opening strict inspector passed
   all ten checks, including related-location semantics.
3. Cris requested changes in review `5118745660`. The author then posted
   [the repair instruction](https://github.com/HBNetwork/demo-pr-readiness/pull/83#issuecomment-5547742993).
   Dan authored and committed `15b910728f2aa63a89256d69043cbc73d0bf8e29`, changing
   exactly the three defective expressions in the two fixture files. History
   recorded `Pushed` and `Replied`, and all six repaired-head jobs passed.
4. Rereview occurrence 296 returned zero findings. Publication occurrence 298
   returned `ReviewLanded`, added no new inline comment, and automatically
   resolved all three original App threads. The independent GitHub snapshot
   and clean History were captured before approval. Initial review and
   publication occurrences were 93 and 95.
5. Cris approved the repaired head in review `5118756832`. Exactly one
   [readiness advisory](https://github.com/HBNetwork/demo-pr-readiness/pull/83#issuecomment-5547763435)
   appeared. The original summary comment `5547729357` became all-clear.
   The final strict inspector passed all ten checks.

The 2,325-record preclosure History contains no `ReplyBlocked`, `ReviewBlocked`,
`ReviewFault`, `RoundUnable`, `ActivityFailed`, or other error terminal. Every
Activity settled. Three `RoundDeferred` terminals and one `ADeferred` terminal
record webhook-custody synchronization. Offline `Engine.load` replay rebuilt
all 2,325 records without running provider effects. Post-closure inspection
also found no errors or pending Activities.

## 1b. Evidence and capture limits

The HTML files are serialized DOM evidence, not self-contained visual pages.
The [2026-09-05 investigation](../../../debt/items/html-proof-captures-omit-assets-and-root-attributes.md)
confirmed external asset dependencies and missing root theme attributes in all
21 original files. A separate [HTML recovery manifest](pr83-html-recovery-manifest.json)
now binds self-contained derivatives for every PR83 HTML file. Open
`tools/demo-video/output/proof/pr83-dark/index.html` to browse the current
dark presentation. The [dark manifest](pr83-dark-manifest.json) links each page
to the earlier recovery and verifies identical offline pixels under light and
dark browser preferences.
Each derivative embeds CSS, fonts and images and passes fresh offline browser
checks. Root attributes are reconstructed and assets were fetched later;
absolute timestamps replace the uncaptured relative-time display. Use accepted
PNGs for contemporaneous appearance. Original files and archive hashes remain
unchanged.

The Navigator subsequently requested removal of the yellow signed-out
conversation banner. All 21 recovered HTML views omit that entire element;
their screenshots, dimensions and package hashes reflect this presentation
edit. Original proof files and reference PNGs retain the original appearance.

The [manifest](pr83-manifest.json) binds 131 local files and the archive by
SHA-256. Evidence lives in `tools/demo-video/output/proof/cv19-pr83/`; the archive
is `tools/demo-video/output/proof/cv19-pr83-proof.tar.gz`. Start with
`03-initial-summary.png`, `12-findings.png`, `14-changes-requested.png`,
`22-repair-request-verified.png`, `33-clear-before-approval.png`, and
`40-all-clear.png`.

The durable export includes canonical History, normalized inbox observations,
review requests, 24 published summary states, and all four completed agent
session transcripts. Three deferred attempts appended no transcript; each
absence has an explicit reason. Secrets and raw webhook payloads are excluded.

Every PNG was visually inspected. `06-reviewing.png` and
`17-repair-requested.png` repeat viewport tiles despite matching page dimensions
and are rejected. The initial summary survives in the accepted first still;
the repair instruction and App commit survive in the accepted replacement.
Later stills use a separate anonymous browser session. The initial still has
one DOM snapshot; subsequent checkpoints retain before/after DOM snapshots.
This recurring [capture-verification gap](../../../debt/items/full-page-capture-dimensions-do-not-detect-repeated-tiles.md)
remains recorded.

`hero-journey.webm` retains 365.8 seconds of browser-viewport recording;
`final-walkthrough.webm` scrolls the final PR for 20.1 seconds. Both fully decode
and contain no audio. They are raw capture evidence: complete real-time playback
review and a separate checkpoint montage are not claimed. Neither recording
captures the macOS desktop. Media acceptance remains a separate CV19 condition.

## 1c. Operational outcome

Production remained active and healthy with `NRestarts=0`, zero scheduler error
classes, and no runnable hints in the final post-closure snapshot. All production
commands used `scripts/ops`, service-account authentication, and pinned SSH,
without an interactive credential prompt. No new runtime defect or refactoring
was found. CV19 remains Active for capture acceptance and the wider repository
monitoring portfolio.

## 1d. Presentation video

A 2:28 silent H.264 demo presents the recovered PR83 screenshots in consistent
dark mode at 1280×720,
30 fps. Sixteen shots follow summary initialization, the three native findings,
the human repair request, App commit, clear rereview, thread resolution,
approval, readiness, and unmerged closure. Captions sit below the evidence;
crops preserve pixels at 1:1 and transitions last 0.8 seconds.

The [demo manifest](pr83-demo-manifest.json) binds the video, renderer,
storyboard, source manifests, previews, and playback review evidence. The
reproduction commands are in the [demo README](../../../../../tools/demo-video/README.md).
Open `tools/demo-video/output/proof/pr83-dark-demo/hamsterdan-pr83-hero.mp4` locally.
The movie compresses elapsed time and uses the authorized recovered views,
including removed sign-in banners, reconstructed theme assets, and the
Navigator-requested explicit dark mode on every page. The original
proof remains the authority for production behavior. Navigator media acceptance
and durable media publication remain separate.
