# 1. PR80 clean production journey

[PR80](https://github.com/HBNetwork/demo-pr-readiness/pull/80) passed the complete
review, repair, rereview, approval, and readiness sequence. It was closed
unmerged after evidence capture. PR78 and PR79 remain discarded runs; their
failures were reproduced and corrected before this run.

Production ran revision `14f41d8a519a8202411e0d1fc1c823949b494cee`, image
`sha256:b016c8aa679b8f376e396c7a3a0c9f1934a9a9f5028aac34ad7a16411a9b5aab`,
with Petrus `4e5c2500af4eb439e8e8f5ec108982c81bfc7427` and OpenAI
`gpt-5.6-sol`. Manual provider checks preceded release qualification and
deployment. The final release gate passed 1,256 tests in parallel and 1,256
serially, with 18 declared platform deselections.

## 1a. Present the journey

1. **A real opening review.** Opening head `ae0a4534c396b1d254b6018e9756f9f0b92ad08f`
   had no comments. All six CI jobs passed. Native review `5118108934` contained
   exactly three App inline comments: seconds incorrectly treated as minutes at
   `gate.py:10`, rejected approvals counted at `gate.py:16`, and inconsistent
   cache normalization at `cache.py:11`, related to `cache.py:6`. All ten strict
   inspector checks passed, including related-location semantics.
2. **Human direction, App repair.** Cris requested changes. The author asked
   Dan to repair all three findings. The App authored and committed
   `d029636252933e76fb3473239eed8eb95c729dc2`; its acknowledgement completed as
   `Replied`. All six jobs passed on that exact repaired head.
3. **Clear before approval.** The second `AgentReview` contained zero findings.
   Its publication completed as `ReviewLanded`, created no new inline comment,
   and resolved all three old App threads. GitHub and the dashboard confirmed
   zero unresolved threads before approval. Initial review/publication
   occurrences were 88/90; clear rereview/publication occurrences were 269/271.
4. **One final advisory.** Cris approved the repaired head in review
   `5118132375`. Exactly one
   [readiness advisory](https://github.com/HBNetwork/demo-pr-readiness/pull/80#issuecomment-5546882464)
   appeared. The dashboard showed all gates clear, and the final strict
   inspector passed all ten checks. Merging remained a human action.

The complete pre-closure History contains 2,250 records and no `ReplyBlocked`,
`ReviewBlocked`, `ReviewFault`, `RoundUnable`, `ActivityFailed`, or other error
terminal. Three `RoundDeferred` results and one readiness `ADeferred` record
represent webhook-custody synchronization. Every Activity settled. The final
closure snapshot also contains no error terminal or pending Activity.

## 1b. Inspect the evidence

The HTML files are serialized DOM evidence, not self-contained visual pages.
The [2026-09-05 investigation](../../../debt/items/html-proof-captures-omit-assets-and-root-attributes.md)
confirmed external asset dependencies and missing root theme attributes in all
12 files. Use accepted PNGs for the contemporaneous appearance. HTML visual
recovery remains open; the original files and archive hashes are unchanged.

The [manifest](pr80-manifest.json) commits artifact paths, sizes, and SHA-256
hashes. Local files live in `tools/demo-video/output/proof/cv19-pr80/`; the
complete local archive is `tools/demo-video/output/proof/cv19-pr80-proof.tar.gz`.
Start with `16-all-clear-verified.png`, then `05-findings.png`,
`07-repair-request.png`, `09-repair-pushed.png`, and
`12-clear-before-approval.png`. `journey.webm` records 361.9 seconds of the
browser viewport; `final-walkthrough.webm` scrolls the final page in 19.9 seconds.

The durable export includes 24 dashboard states, review requests, normalized
inbox observations, all History, and four agent sessions covering the completed
work. Two deferred attempts were cancelled before a transcript append; another
was deferred before creating a body. Their absent sessions are explicit export
gaps. Raw webhook payloads were excluded under the Navigator's instruction.
No credentials or local credential stores are included.

Visual review rejected tiled PNGs at the standalone request-changes checkpoint
and the first final capture. Their DOM/API snapshots remain, the following
repair-request still shows both human instructions, and the final still was
recaptured and inspected successfully. The opening navigation initially failed;
the opening API snapshot and recovered pre-comment page are preserved. These
limits are recorded rather than treating image dimensions alone as visual
verification. The recordings show the browser viewport, not the macOS desktop.

## 1c. Operational outcome

Production remained active with `NRestarts=0`, health `ok`, and zero scheduler
error classes. Fingerprint-pinned SSH and the operations service account worked
without an interactive 1Password prompt. `deployment.observe` now provides
filterable JSON lines across workflow History, Motus dispatch, webhook custody,
and health; its command and exclusions are documented in `deployment/README.md`.

CV19 remains Active for Navigator review of the capture package and the wider
repository-monitoring requirement. Production still admits one repository.
