# 1. PR84 preserves the PR83 hero journey after the secrets migration

[PR84](https://github.com/HBNetwork/demo-pr-readiness/pull/84) passed the complete
hero journey on production release `85c109eb0099ce1aad32d041c9ca69492f3b1272`.
It was closed unmerged after capture. The comparison uses [PR83](pr83.md),
which remains untouched.

The deployed image is
`sha256:de64787a2b4b39b0535bd4a3c71cdecdd306ab753f8b842bf613a63738cea071`,
with Petrus `9ad2f7a8daac7aee03ba9529f65e18894aa8f608` and OpenAI `gpt-5.6-sol`.
Production retrieves its credentials from `hamsterdan-prod` on every start.
[RS-037](../../../workbench/rs-037-single-authority-secrets.md) owns deployment,
recovery, startup-failure checks and remaining remote Amp migration.

## 1a. Behavioral comparison

| Checkpoint | PR83 | PR84 |
| --- | --- | --- |
| Opening fixture | Parent `a83e922`, tree `9768333` | Identical parent and complete tree |
| Initial summary | 14 seconds after opening | 13 seconds after opening |
| Summary, startup completion, first normal Activity | History sequences 318, 341, 345 | Same sequences |
| Opening CI | Six jobs passed | Six jobs passed |
| Opening review | Three findings at `gate.py:10`, `gate.py:16`, `cache.py:11`, with cache related location at line 6 | Same defects and locations; strict inspector passed all ten checks |
| Human instruction | Cris requested changes; Henrique requested all three repairs | Same distinct identities and exact review/instruction bodies |
| App repair | Three expression changes in two files, tree `06dfb22` | Identical patch and complete repaired tree |
| Repaired CI | Six jobs passed | Six jobs passed |
| Rereview before approval | Zero findings; three App threads automatically resolved | Same, independently verified through GitHub and History before approval |
| Human approval | Cris approved the repaired head | Same identity and exact approval body |
| Readiness | One advisory; original summary all-clear | Same |
| Preclosure failures / pending Activities | 0 / 0 | 0 / 0 |
| Offline replay | 2,325 records, no effects | 2,390 records, no effects |
| Final disposition | Closed unmerged | Closed unmerged |

The new opening head is `1fe175f695ed3ff17aeea3b3431b1600362ab7db`.
Hamsterdan authored and committed repair `d9bcb40d988da6bc77ca478b7d99cc2b18dab15e`.
There was no manual fixture repair or manual thread resolution.

PR84's initial review and publication occurrences are 99 and 101. Its clear
rereview and publication occurrences are 282 and 284. The GitHub snapshot
before approval records all three threads resolved by `hamster-dan[bot]`, six
successful checks, and no approval yet. Cris's approval is review `5128296665`.
The original summary is comment `5565392417`; the sole readiness advisory is
comment `5565443716`.

Prose, finding order, commit title and timing varied. PR84 has 25 summary
publications rather than 24 and five review deferrals rather than three. Both
runs have one advisory deferral. Each PR84 deferral names a pending webhook
delivery; none is a failed Activity. PR84 settled all 38 requested Activities
and all 411 started firings before closure. Two reviews, two review
publications, one push, one reply and one readiness publication match PR83.

## 1b. Evidence and limits

The [manifest](pr84-manifest.json) binds the ignored local evidence under
`tools/demo-video/output/proof/rs037/pr84/` and its archive. It includes staged
GitHub snapshots, canonical History, payload-free runtime projections, strict
inspectors, source-tree and patch comparisons, replay checks and full-page
screenshots. The replay script refuses any Dispatch call and verifies that
loading leaves History unchanged.

Use `06-initial-summary-visible.png`, `11-findings.png`,
`18-repair-request.png`, `21-app-repair.png`, `31-all-clear.png` and
`34-closed.png` for the visually checked journey. They show no repeated viewport
tiles. Earlier captures `02` and `04` precede visible summary publication;
`15` still displays the preceding review state. They are retained but rejected
for their intended captions. Capture `25` shows the App repair while the summary
publication is still queued. It does not prove a visibly clear dashboard before
approval; the independent API snapshot and History prove clear rereview and
thread resolution at that checkpoint. The final screenshot verifies the settled
all-clear dashboard.

This is a functional comparison package. No new video, self-contained HTML
capture, complete agent-session transcript export or issuer credential rotation
is claimed. Those are unnecessary to the observed GitHub and workflow outcome.

## 1c. Production outcome

Post-closure History contains 2,461 records, no failed or pending Activities,
and replays without effects. Production has zero scheduler error classes,
zero runnable hints, all 681 inbox rows terminal and `NRestarts=0`. The 20
pre-migration PR histories retain their original bytes. Application secrets
remain absent from Docker metadata. Private recovery custody remains available.

No runtime fix or additional refactoring was needed during this comparison.
The existing screenshot freshness and candidate-retention limitations remain
explicit. The Navigator accepted the production comparison. RS-037 remains
Active for remote Amp migration; this result does not close CV19's separate
media or monitoring scope.
