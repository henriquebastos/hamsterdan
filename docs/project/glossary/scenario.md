# Scenario

One named end-to-end story run as a test: feed a sequence of webhooks into a fake GitHub, let the app run until quiet, then assert exactly which comments exist and which API calls were and were not made. Eleven scenarios cover the current PR workflow.

Avoid: semantic journey, user journey

Related: [PR Workflow](pr-workflow.md)
